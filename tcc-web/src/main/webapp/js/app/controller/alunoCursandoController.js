tccApp.controller('AlunoCursandoController', ['$scope',
    function ($scope) {
        $scope.etapa1='img/tabuleiro/1.png';
        $scope.model = {
        };

        var init = function () {
            
        };
        init();
    }]);