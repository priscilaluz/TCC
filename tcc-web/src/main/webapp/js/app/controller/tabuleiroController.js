tccApp.controller('TabuleiroController', ['$scope',
    function ($scope) {
        $scope.etapa1='img/tabuleiro/1-1.png';
        $scope.model = {
        };

        var init = function () {
            
        };
        init();
    }]);